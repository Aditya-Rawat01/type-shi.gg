export const URI = 'https://type-shi.ardev.in' 
// check for better auth url at the .env file as well.
// http://localhost:3000
// change the auth callback at the google cloud console and github so the oAuth works correctly.
